# CLAUDE.md

이 저장소는 도서 『반도체 데이터 엔지니어링(가제)』(한빛아카데미, 5부 13장)의 공식 실습 코드 저장소다.

**이 저장소에서 하는 일은 실습 코드 작성·검증·정리다.** 교재 본문 집필과 원고 검토는 claude.ai 프로젝트에서 별도로 진행하므로, 여기서 원고 문장이나 이미지 프롬프트를 생성하지 않는다.

---

## 개발 환경

- Windows + PowerShell. 터미널 명령은 PowerShell 문법으로 제시한다.
- Python 3.14 (최소 3.12). 항상 가상환경을 활성화한 상태에서 실행한다.
- 가상환경 폴더명은 **반드시 `.venv`** 다. `.vscode/settings.json`이 인터프리터 경로를 `${workspaceFolder}/.venv`로 고정하고 있어, 다른 이름을 쓰면 VS Code가 시스템 파이썬을 잡는다.
- 패키지는 `requirements.txt`에 고정되어 있다. **명세에 없는 패키지를 임의로 추가하지 않는다.** 필요하면 먼저 알린다.

```powershell
.\.venv\Scripts\Activate.ps1
python check_environment.py
```

---

## 폴더 및 파일명 규칙

교재 목차와 저장소 구조가 1:1로 대응한다. 폴더명은 **전부 소문자**다.

```
.vscode/                     # 공유 워크스페이스 설정(1.2절). 개인 설정을 넣지 않는다
data/                        # 데이터셋, 제너레이터, 13장 RAG용 가상 매뉴얼(manuals/)
part1_python_automation/     ch01/ ch02/ ch03/
part2_process_analysis/      ch04/ ch05/ ch06/
part3_visual_db_report/      ch07/ ch08/ ch09/
part4_ml_prediction/         ch10/ ch11/
part5_rag_capstone/          ch12/ ch13/
```

실습 코드 파일명은 `chXX_YY_기능이름.py` 형식이다. XX는 장, YY는 절 번호다.

- 예: `part1_python_automation/ch01/ch01_02_remote_check.py` (1장 2절)
- 새 파일을 만들 때 해당 절이 실재하는지 목차로 확인한다. 임의의 절 번호를 만들지 않는다.
- **예외: 여러 절이 함께 불러 쓰는 공통 모듈은 절 번호 없이 `ax_` 접두어를 쓴다.** 실행 스크립트가 아니라 부품이며, 표준 라이브러리와 이름이 충돌하지 않게 하기 위해서다.
  - 예: `part1_python_automation/ch03/ax_settings.py`, `ax_sta.py`, `ax_summary.py`
  - 3장부터 경로·관찰 기준은 `ax_settings.py`, 판정·요약은 `ax_summary.py`의 것을 불러 쓴다. 같은 규칙을 다른 파일에 다시 작성하지 않는다.

---

## 통합 데이터셋 (변경 금지 영역)

`data/data_generator.py`가 생성하는 5,000행 CSV는 교재 전체가 공유하는 단일 데이터셋이다. **생성 규칙을 임의로 바꾸지 않는다.** 수치가 바뀌면 이미 집필된 본문의 실행 결과가 전부 틀어진다.

- 시드 `np.random.seed(42)` 고정. 절대 변경하지 않는다.
- 구조: 10로트 × 5웨이퍼 × 100다이. 장비는 웨이퍼 단위로 배정된다.
- 컬럼 16개: `Timestamp, Lot_ID, Wafer_ID, Die_X, Die_Y, Equipment_ID, Path_ID, Voltage, Chamber_Temp, Pressure, Gas_Flow, Operating_Freq, Worst_Slack, Leakage_Power, Pass_Fail, Error_Code`
- `Leakage_Power`에 결측 2.5%(125행)가 의도적으로 포함되어 있다. 채우거나 제거하지 않는다.
- `Error_Code` 값은 `NORMAL` / `ERR-T101`(슬랙 음수) / `ERR-G202`(가스 유량 저하 경고) 세 가지다.
- 수집 구간은 `2026-01-01 08:00 ~ 2026-01-18 16:35`(전체 5분 간격)이다. 장비는 웨이퍼 단위로 배정되므로 장비별로 추리면 간격이 균일하지 않다. "같은 장비의 다음 N행 = N×5분 뒤"로 가정하지 않는다.

### 검증 지표

제너레이터를 수정한 뒤에는 아래 값이 그대로 나오는지 반드시 확인한다. 하나라도 달라지면 되돌린다.

<details>
<summary>⚠️ 스포일러 주의: 교재 6장에서 독자가 직접 규명할 원인이 들어 있습니다 (집필·검증용). 학습자라면 6장을 마친 뒤에 펼쳐 보기를 권합니다.</summary>

| 항목 | 값 |
|---|---|
| 전체 행 수 / 컬럼 수 | 5,000행 / 16열 |
| 전체 불량률 | 16.08% (804행) |
| ETCHER_A / ETCHER_B 불량률 | 1.04% / 28.89% |
| 에러 코드 분포 | NORMAL 4,023 / ERR-T101 804 / ERR-G202 173 |
| Leakage_Power 결측 | 125행 |
| 로트별 불량률 | LOT_00 0.2% → LOT_09 62.2% |
| 불량 발생 온도 구간 | 76.08 ~ 97.90℃ (85℃ 초과 270행은 전량 불량) |
| 정상 판정 최고 온도 | 84.24℃ |

두 가지 불변 조건도 함께 확인한다. 불량 판정(`Pass_Fail == 0`)인 행의 에러 코드는 모두 `ERR-T101`이어야 하고, `ERR-G202`가 붙은 173행은 모두 양품이어야 한다. 제너레이터가 슬랙으로 양불을 먼저 판정하고 양품에만 가스 유량 경고를 기록하기 때문이다. 따라서 `ERR-G202`가 없다고 가스 유량 저하가 없었던 것은 아니며, `ERR-G202`는 검증된 고장 선행 신호가 아니라 상태 경고로 다룬다.

1~5장 코드에서 지켜야 할 구체적 금지 사항: `ETCHER_B`를 원인으로 서술하지 않고, 온도 임계선(85℃)을 상수로 박아 두지 않는다.

</details>

CSV를 직접 편집하지 않는다. 데이터가 필요하면 제너레이터를 실행해 재생성한다.

---

## 코드 작성 규칙

- **작성한 코드는 반드시 실행해 확인한다.** 실행 결과를 추정해서 보고하지 않는다.
- 코드는 두 벌로 관리한다. 저장소에는 **줄 번호 없이 바로 실행되는 `.py`** 만 커밋한다. 지면용(줄 번호 포함) 버전은 원고 쪽에서 따로 만든다.
- **한 줄은 72자 이내로 쓴다.** 지면 폭을 넘기면 책에서 줄이 접혀 줄 번호와 코드가 어긋난다. 길어지면 괄호 안에서 줄을 나눈다.
- 터미널에 한글을 출력하는 코드는 **표시 폭을 계산해 정렬한다.** 한글은 영문의 두 배 폭을 차지하므로 `ljust`나 `:<n` 서식만으로는 열이 맞지 않는다(`ch01_02_remote_check.py`의 `pad()` 참고).
- API 키는 `.env` + `python-dotenv`로만 다룬다. 하드코딩하지 않으며 `.env`는 커밋하지 않는다. **키가 없어도 실행되는 형태**로 작성한다. 1.5절 실습은 키 없이 진행할 수 있어야 한다.
- matplotlib 사용 시 한글 폰트 설정과 `rcParams["axes.unicode_minus"] = False`를 반드시 포함한다. 이 책은 음수 슬랙이 핵심 결과라 부호가 깨지면 안 된다.
- 파일 순회 중 이름 변경·삭제가 필요하면 `list()`로 감싸 순회 대상을 먼저 확정한다.
- pandas 3.x 기준으로 작성한다. Copy-on-Write가 기본 동작이고 문자열 컬럼의 기본 dtype이 2.x와 다르므로, 옛 관행(`inplace`, 연쇄 대입)을 그대로 쓰지 않는다.
- 포매터는 `black`, 린터는 `pylint`를 따른다.

### 서사 순서 지키기

교재는 수율 급락의 원인을 6장에서 규명하는 구조다. **1~5장 코드와 주석에서 원인을 미리 지목하지 않는다.**

- 1~5장 코드와 주석에서 특정 장비를 원인으로 지목하거나 원인과 관련된 임계값을 상수로 박아 두지 않는다. 구체적인 대상은 위 검증 지표의 스포일러 블록에 있다.
- 장비별 불량률 비교, 온도 구간별 불량률 산출은 6장 코드부터 등장시킨다.
- 1~5장에서 노출 가능한 범위는 전체 불량률 16.08%와 로트별 추이까지다. 실행 출력(print)도 같은 범위를 지킨다.
- 이 파일과 `README.md`는 공개된다. 원인 정보가 필요하면 `<details>` 스포일러 블록 안에만 적는다.

---

## 커밋

- 메시지는 한국어로, 어느 절의 작업인지 드러나게 쓴다.
  - 예: `1.3절 가상환경 격리 진단 스크립트 추가`
  - 예: `data_generator 저장 경로를 스크립트 기준으로 변경`
- 한 커밋에 여러 절의 변경을 섞지 않는다.
- `.venv/`, `.env`, `__pycache__/`는 커밋하지 않는다.
- **생성물은 커밋하지 않는다.** 통합 CSV, `data/logs/`, `data/logs_mixed/`, `data/parsed/`, `data/parsed_engine/`, `data/parsed_stale/`은 시드가 고정된 스크립트로 언제든 재현되므로 루트 `.gitignore`에 등록되어 있다. 새 생성물 폴더를 만들면 `.gitignore`에도 추가한다.

### 절 작업을 마친 뒤

1. 해당 절의 `.py`를 실행해 결과를 확인한다.
2. `black`과 `pylint`를 통과시킨다.
3. `README.md`의 **갱신 이력** 표에 날짜·대상·내용을 한 줄 추가한다. 이 저장소는 출간 이후에도 라이브러리 변화를 흡수해 독자에게 최신 코드를 제공하는 것이 목적이므로, 이력 기록을 빠뜨리지 않는다.